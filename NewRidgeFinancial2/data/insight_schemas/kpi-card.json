{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NR2 KPI Card Insight",
  "type": "object",
  "required": ["widget_type", "title", "data", "source_refs", "confidence"],
  "additionalProperties": false,
  "properties": {
    "widget_type": { "const": "kpi-card" },
    "title": { "type": "string", "minLength": 1, "maxLength": 120 },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "value": { "type": ["number", "null"] },
        "unit": { "type": "string", "enum": ["dollars", "percent", "count", "days", "text"] },
        "trend_direction": { "type": "string", "enum": ["up", "down", "flat", "unknown"] },
        "trend_percent": { "type": ["number", "null"] },
        "benchmark": { "type": ["number", "null"] },
        "label": { "type": "string", "maxLength": 80 }
      }
    },
    "source_refs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "string",
        "pattern": "^(softdent|qb|nr2|import):[a-z0-9_./-]+:[0-9]{4}-[0-9]{2}(-[0-9]{2})?$"
      }
    },
    "confidence": { "type": "string", "enum": ["high", "medium", "low"] },
    "explanation": { "type": "string", "maxLength": 280 },
    "action_cta": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "label": { "type": "string", "maxLength": 60 },
        "route": { "type": "string", "enum": ["financial", "claims", "ar", "softdent", "quickbooks", "hal", "office-manager", "taxes"] }
      }
    }
  }
}
